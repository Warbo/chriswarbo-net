#! /usr/bin/env nix-shell
#! nix-shell -i bash -p pythonPackages.pyyaml -p jq

function y2j {
    IMPORTS='import sys, yaml, json'
    CONVERT='json.dump(yaml.load(sys.stdin), sys.stdout, indent=4)'
    python -c "$IMPORTS; $CONVERT"
}

y2j | jq "$@"
