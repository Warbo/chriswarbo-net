#!/usr/bin/env bash

function fail {
    echo "FAIL: $1" 1>&2
    exit 1
}

OUTPUT=$(echo '<pre><code></code></pre>' | render_page)

echo "$OUTPUT" | grep '<code>' > /dev/null && fail "Didn't strip <code>"
echo "$OUTPUT" | grep '<pre>'  > /dev/null && fail "Didn't strip <pre>"

echo "Success" 1>&2
