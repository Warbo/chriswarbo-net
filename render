#! /usr/bin/env nix-shell
#! nix-shell -i bash -p gnumake bash pandoc

printf "Waiting for rendering lock..." 1>&2
(
flock -x 200
printf "lock aquired.\n" 1>&2

make -j2 -f Makefile "$@"

) 200>/tmp/blog.lock

